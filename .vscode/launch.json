{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Process",
      "type": "lldb",
      "request": "attach",
      "pid": "${command:pickProcess}",  // This will allow you to select the process from a list
      "stopOnEntry": true,
    }
  ]
}
